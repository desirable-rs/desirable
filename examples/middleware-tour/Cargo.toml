[package]
edition = "2024"
name = "example-middleware-tour"
version = "0.1.0"

[dependencies]
desirable = { path = "../../", features = ["compression"] }
serde_json = "1"
tokio = { version = "1", features = ["full"] }
